import { ConsultationPipe } from './consultation.pipe';

describe('ConsultationPipe', () => {
  it('create an instance', () => {
    const pipe = new ConsultationPipe();
    expect(pipe).toBeTruthy();
  });
});
